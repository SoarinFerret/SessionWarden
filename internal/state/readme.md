# state.json Management

This module handles reading and writing the session state to a JSON file located at `/var/lib/sessionwarden/state.json`. This file stores information about user sessions, including total usage time for the day and any active overrides.

## State Structure

See `./state.json` for an example implementation.
